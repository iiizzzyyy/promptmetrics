# Build Tasks: Issue #23 -- Restore CLI `--json` Output Flag

**Derived from:** [Issue Resolution Plan](./issue-23-resolution-plan.md)  
**Date:** 2026-04-26  
**Status:** Complete  
**Milestone:** [CLI Stability](https://github.com/iiizzzyyy/promptmetrics/milestone/4)

---

## Legend

- `[ ]` -- Not started
- `[-]` -- In progress
- `[x]` -- Complete
- `**BLOCKED**` -- Waiting on dependency

---

## Epic 1: Add Global `--json` Flag and Output Helper

**GitHub Issue:** [#23 -- CLI removed --json flag breaking programmatic use and integration tests](https://github.com/iiizzzyyy/promptmetrics/issues/23)  
**Goal:** Restore `--json` support across all CLI commands with a centralised output helper.

---

### Task 1.1 -- Register Global `--json` Option

- [x] **1.1.1** Open `src/cli/promptmetrics-cli.ts`
- [x] **1.1.2** After line 106 (`program.option('--server <url>', ...)`), add:
  ```typescript
  program.option('--json', 'Output as JSON');
  ```
- [x] **1.1.3** Verify the option appears in `--help` output

---

### Task 1.2 -- Create Centralised `print()` Helper

- [x] **1.2.1** In `src/cli/promptmetrics-cli.ts`, add `print()` helper after `getHeaders()`:
  ```typescript
  function print(data: unknown): void {
    const opts = program.opts() as { json?: boolean };
    if (opts.json) {
      console.log(JSON.stringify(data, null, 2));
      return;
    }
    if (Array.isArray(data)) {
      console.table(data);
      return;
    }
    console.log(data);
  }
  ```
- [x] **1.2.2** Verify TypeScript compiles: `npm run build` -- Clean

---

### Task 1.3 -- Update `list-prompts` Command

- [x] **1.3.1** In `src/cli/promptmetrics-cli.ts`, find the `list-prompts` action
- [x] **1.3.2** Replace `console.table(...)` with `print(...)`

---

### Task 1.4 -- Update `import` Command

- [x] **1.4.1** In `src/cli/promptmetrics-cli.ts`, find the `import` action
- [x] **1.4.2** Replace `console.table(results)` with `print(results)`

---

### Task 1.5 -- Update Commands Already Outputting JSON

- [x] **1.5.1** Replaced `console.log(JSON.stringify(res.data, null, 2))` with `print(res.data)` for:
  - `create-prompt`
  - `get-prompt`
  - `log`
  - `create-trace`
  - `get-trace`
  - `add-span`
  - `create-run`
  - `update-run`
  - `add-label`
  - `get-label`
- [x] **1.5.2** For `export` command, changed to `print({ exported: prompts.length, directory: options.out })`
- [x] **1.5.3** For `init` command, kept plain string output (no structured data to emit)

---

## Epic 2: Unit Test Coverage

**Goal:** Verify `--json` works correctly and default output is unchanged.

---

### Task 2.1 -- Add `--json` Tests

- [x] **2.1.1** Open `tests/unit/cli.test.ts`
- [x] **2.1.2** Add test: `list-prompts --json outputs valid JSON`
- [x] **2.1.3** Add test: `list-prompts without --json uses console.table`
- [x] **2.1.4** Add test: `create-prompt accepts --json flag`

---

### Task 2.2 -- Run Unit Tests

- [x] **2.2.1** Run CLI tests: `npx jest tests/unit/cli.test.ts`
- [x] **2.2.2** Verify all tests pass -- 18 passed, 0 failed

---

## Epic 3: Regression Testing

**Goal:** Ensure no other tests break.

---

### Task 3.1 -- Full Test Suite

- [x] **3.1.1** Run full test suite: `npm test`
- [x] **3.1.2** Verify exit code 0 -- 31 suites passed, 215 tests
- [x] **3.1.3** Run build: `npm run build`
- [x] **3.1.4** Verify TypeScript compiles cleanly -- Clean

---

### Task 3.2 -- Manual CLI Smoke Test

- [x] **3.2.1** Build the CLI: `npm run build` -- Clean
- [x] **3.2.2** Test `--json` flag on list-prompts -- Flag accepted, JSON output valid
- [x] **3.2.3** Test without `--json` -- Table format preserved
- [x] **3.2.4** Test `--json` on get-prompt -- Flag accepted, JSON output valid

---

## Epic 4: Final Commit

- [x] **4.1.1** Committed:
  ```bash
  git commit -m "fix(cli): restore --json output flag for programmatic consumers (#23)"
  ```

---

## Cross-Cutting Checklist

- [x] `--json` flag is accepted on all commands that previously supported it
- [x] Output is valid, parseable JSON when `--json` is set
- [x] Default output (without `--json`) remains human-readable table format
- [x] `npm test` passes with exit code 0
- [x] `npm run build` compiles cleanly

---

## Test Results

| Suite | Result | Time |
|-------|--------|------|
| `npx jest tests/unit/cli.test.ts` | 18 passed, 0 failed | 2.6 s |
| `npm test` (all suites) | 31 passed, 215 tests | 11.2 s |
| `npm run build` | Clean (no errors) | <2 s |

---

*Tasks generated by AI Engineer & Software Architect agent analysis.*
*Implementation completed 2026-04-26.*
